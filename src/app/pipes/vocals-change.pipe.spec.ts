import { VocalsChangePipe } from './vocals-change.pipe';

describe('VocalsChangePipe', () => {
  it('create an instance', () => {
    const pipe = new VocalsChangePipe();
    expect(pipe).toBeTruthy();
  });
});
